package version

var (
	// BuildTime is a time label of the moment the binary was built
	BuildTime = "unset"
	// Commit is a last commit hash at the moment the binary was built
	Commit = "unset"
	// Release is a semantic version of the current build
	Release = "unset"
)
